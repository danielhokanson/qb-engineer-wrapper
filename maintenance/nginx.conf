server {
    listen 80 default_server;
    server_name _;
    root /usr/share/nginx/html;

    # Every route shows the maintenance page
    location / {
        try_files /maintenance.html =503;
        add_header Cache-Control "no-store" always;
    }
}
