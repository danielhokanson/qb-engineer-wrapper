import { Routes } from '@angular/router';
import { CustomerReturnsComponent } from './customer-returns.component';

export const CUSTOMER_RETURNS_ROUTES: Routes = [{ path: '', component: CustomerReturnsComponent }];
