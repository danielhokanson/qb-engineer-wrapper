import { Routes } from '@angular/router';

import { HeadlessFormRenderComponent } from './headless-form-render.component';

export const RENDER_ROUTES: Routes = [
  { path: '', component: HeadlessFormRenderComponent },
];
