FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# Serve on port 80 to match production compose port mapping (4200:80)
EXPOSE 80
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--port", "80", "--poll", "2000"]
