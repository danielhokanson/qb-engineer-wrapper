# Demo-data JSON files are generated by the export stack:
#   ./export-demo-data.sh  (or .ps1)
#
# Generated files are tracked in git so the Angular demo build picks them up
# without anyone needing to run the export locally.
